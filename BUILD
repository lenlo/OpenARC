# OPENARC BUILD INSTRUCTIONS
# ==========================

# To build and install OpenARC, first make sure that the prerequisits are
# satisfied:

sudo apt install -yy make automake pkg-config libtool m4 libbsd-dev libssl-dev libmilter-dev

# Then do:

aclocal && autoconf && autoreconf --install && automake --add-missing && ./configure && make all

# Followed by:

make

# To install
echo 'run "make install" to install into /usr/local"
